# 更新记录

本文件记录各版本的主要变化。GitHub Release 同步提供对应版本的更新说明。

## [未发布]

### 新增

- 增加 `autopid` P0 包和严格 YAML/JSON 配置校验，兼容现有网页网关常用字段。
- 增加标准 PID Controller 只读适配器和 Block/Port/Signal 图。
- 增加负反馈信号角色识别：参考、误差、反馈、PID 原始输出和执行器控制量。
- 增加级联内外环识别和“先内环、后外环”建议顺序。
- 增加 NIAE、NISE、NITAE、NRMSE 等归一化指标、原始纹波和稳定性分类。
- 增加仿真错误、非有限值、发散、输出/控制/电流/电压、饱和、超调、调节时间和稳态误差硬门禁。
- 增加 Mock Runner、MATLAB JSON 桥、P0 CLI、示例配置和自动化验收测试。
- 增加当前状态审计、P0 架构和调研记录。
- 增加主评价 PID 和参考、反馈、控制、电流保护信号的人工选择与确认。
- 模型扫描可从误差求和块和 `To Workspace` 记录关系生成信号建议与置信度。
- 启动前检查所选信号是否已记录，参考或控制信号读取失败时终止该次评价。
- 每个选中 PID 使用独立评价环，可分别设置参考、反馈、控制、电流信号、权重、限幅和目标。
- 级联双环增加内环、外环、联合微调三个阶段，外环调节时继续检查内环。
- 增加多设定值分段评价、RMSE、电流峰值、输出纹波、饱和率和归一化综合分数。
- 增加模型参数应用、自动备份、回滚和 SHA-256 模型指纹保护。
- 多 PID 管理器保存逐环评价配置，并只根据明确的信号连接证据自动分组。

### 修改

- 网页界面改为接近 MATLAB/Simulink 的工具条、表格和工作区布局。
- 含多个 PID 的模型读取后不再默认选择前两个控制器。
- Buck 按钮改为读取并检查示例模型，不再直接启动硬编码双环任务。
- 网关健康检查改为后台缓存，页面刷新不再等待 MATLAB 探测完成。
- 服务开发版本更新为 `0.5.0-dev`。

### 修复

- 修复初始时刻参考阶跃的超调量被计算为零的问题。
- 修复多阶段搜索记录预分配失败和新阶段首个候选中心错误。
- 修复存在 `logsout` 时无法回退读取 `To Workspace` 信号的问题。
- 修复向量信号被静默取第一列和重复记录信号名产生歧义的问题。
- 修复标准 Simulink 库链接 PID 无法应用参数的问题。
- 修复过期任务结果可能写入已修改模型的问题。

### 验证

- 35 项 Python 接口、配置、AI 适配和前端打包测试通过。
- 6 项 MATLAB 逐环评价、级联阶段、候选冻结、超调和应用/回滚测试通过。
- 5 项 MATLAB 模型扫描测试、2 项内嵌界面测试和多 PID 管理器测试通过。
- 完成一次网页到 MATLAB、Simulink 仿真、指标计算、历史记录和状态回传的端到端任务。
- 浏览器验证多 PID 模型默认零选中，并可显示两套独立评价配置。

### 约束

- P0 只扫描和验证，不实现参数优化、LLM 接入或自动写回。
- 静态拓扑结果在没有动态探测时只建议人工确认。
- 原始 Simulink 模型禁止覆盖。

## [v0.4.0] - 2026-07-27

### 新增

- 每次调参先仿真原始 PID，保存为效果基线。
- 增加原始基线、当前候选、当前最佳和目标上限对比表。
- 增加调参结论和等级，显示硬指标是否通过及失败原因。
- 为四个 Simulink Toolstrip 操作增加独立图标。

### 修改

- 内嵌网页通过 MATLAB 受控代理访问本地网关，避免 `uihtml` 直接请求本地 URL 失败。
- Code Agent 发现和测试错误改为明确的网关或 CLI 状态提示。
- 候选来源和 AI 配置窗口显示更清楚。
- 实时状态文件增加基线记录和目标指标快照。

### 修复

- 修复 `PID Agent` 标签存在但按钮区域为空白的问题。
- 修复 AI 配置中 Code Agent 发现显示 `Failed to fetch` 的问题。
- 修复前端引用已删除的指标容器导致初始化中断的问题。
- 安装器会检测并移除来自其他目录的同名 Toolstrip 组件。

### 验证

- 9 项 Python 自动测试通过。
- MATLAB Code Analyzer 对修改的 MATLAB 文件报告 0 条问题。
- MATLAB 基线仿真、状态文件和目标快照验证通过。
- MATLAB `uihtml` 桥接和本地网关代理验证通过。
- Simulink Toolstrip 组件和 8 个图标文件加载验证通过。
- 检查效果判定页和 Code Agent 发现界面，浏览器控制台无错误。

## [v0.3.0] - 2026-07-25

### 新增

- 在 Simulink 中增加 `PID Agent` 工具栏。
- 支持从当前模型直接打开内嵌网页调参界面。
- 支持读取模型中已选中的一个或两个 PID，并在网页中自动选中。
- 增加当前模型、多 PID 管理器和调参历史入口。
- 增加从网页定位到 Simulink PID 块的操作。

### 修改

- MATLAB 自动启动或复用本地网关，不再要求先运行批处理文件。
- 内嵌网页固定使用当前模型，并同步工作目录和 MATLAB Project 路径。
- 模型扫描包含 Variant 中的 PID 块。
- 多 PID 启动库选择模型后，使用同一内嵌调参界面。
- 任务列表忽略没有状态文件的不完整目录。

### 验证

- 9 项 Python 自动测试通过。
- 2 项 MATLAB 当前模型和双 PID 选择测试通过。
- MATLAB 与内嵌网页桥接测试通过。
- MATLAB Code Analyzer 对本次 MATLAB 文件报告 0 条问题。
- 检查普通浏览器宽屏和窄屏布局。

## [v0.2.1] - 2026-07-25

### 新增

- 支持通过 Qwen Code 生成 PID 候选参数。
- 支持通过 Kimi Code CLI 生成 PID 候选参数。
- 支持通过 CodeBuddy Code 生成 PID 候选参数。
- 前端可以发现、选择和测试上述 Code Agent。

### 修改

- Code Agent 请求直接传递参数数据，不再要求工具读取调参任务文件。
- Qwen Code 和 Kimi Code CLI 使用只读规划模式。
- CodeBuddy Code 使用规划模式，并禁用命令执行、文件修改和网络搜索工具。
- 改进不同 Code Agent 返回格式的 JSON 解析。

### 验证

- 增加三种 Code Agent 的调用参数和返回数据自动测试。
- 检查 Python 代码语法和前端 JavaScript 语法。

## [v0.2.0] - 2026-07-25

### 新增

- 支持扫描模型中的多个 PID 控制器。
- 支持创建调参计划，并按系统或控制环分组。
- 支持在 Simulink 启动库中选择待调 PID。
- 增加多系统、多 PID 示例模型。

### 修改

- 简化 README 说明。
- 公开仓库不上传调参任务数据和用户模型。

## [v0.1.0] - 2026-07-12

### 新增

- 提供 PID 调参控制台、Simulink 仿真验证和历史记录。
- 支持远程 API、Codex CLI、MiniMax Code 和 Claude Code。
- 提供单 PID、双 PID 和 Buck 双环示例。
